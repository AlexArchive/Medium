@model GoBlog.Infrastructure.Paging.PagedList<PostViewModel>

@{
    ViewBag.Title = "Index";
}

@foreach (var post in Model) {
    <h2>@Html.ActionLink(post.Title, "Post", new { slug = post.Slug })</h2>
    <p>@post.Summary</p>
}

@Html.Partial("_PagerPartial")
