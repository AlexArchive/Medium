@model IEnumerable<PostViewModel>

@{
    ViewBag.Title = "Admin";
}

@Html.ActionLink("Logout", "Logout", "Login")
@Html.ActionLink("Add", "Add", "Home")

<table>
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Title)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Published)
            </th>
        </tr>
    </thead>
    
    <tbody>
        @foreach (var post in Model) {
            <tr>
                <td>
                    @Html.DisplayFor(_ => post.Title)
                </td>
                <td>
                    @Html.DisplayFor(_ => post.Published)
                </td>
                <td>
                    @Html.ActionLink("Delete", "Delete", "Home", new { slug = post.Slug }, new{})
                    @Html.ActionLink("Edit", "Edit", "Home", new { slug = post.Slug }, new { })
                    @Html.ActionLink("Preview", "Post", "Home", new { area = "", slug = post.Slug }, new { })
                </td>
            </tr>
        }
    </tbody>
</table>